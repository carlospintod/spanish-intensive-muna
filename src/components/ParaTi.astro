---
interface Props { text: string }
const { text } = Astro.props;
---
<aside class="mb-6 rounded-2xl border border-terracotta-100 bg-terracotta-50/60 p-5 text-ink-900 sm:p-6">
  <p class="section-eyebrow">Para ti, Muna</p>
  <p class="mt-2 text-base leading-relaxed sm:text-lg">{text}</p>
</aside>
